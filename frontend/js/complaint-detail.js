document.addEventListener('DOMContentLoaded', () => {
    // In real app, parse URL params: new URLSearchParams(window.location.search).get('id')
    // and fetch data via window.api.getComplaint(id)
    
    // For now, static mock data is already in HTML.
    // Just initialize icons.
    lucide.createIcons();
});
